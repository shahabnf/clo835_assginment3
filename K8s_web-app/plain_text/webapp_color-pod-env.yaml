apiVersion: v1
kind: Pod
metadata:
  name: webapp-color-env
  labels: 
    app: webapp
spec:
  containers:
  - image: mmumshad/simple-webapp-color
    env:
    - name: APP_COLOR
      value: "pink"
    name: webapp-color-plainText
    ports:
    - containerPort: 8080
      protocol: TCP

